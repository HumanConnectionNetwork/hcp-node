services:
  hcp-client-telegram:
    build: .

    container_name: hcp-client-telegram

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    command: python main.py
